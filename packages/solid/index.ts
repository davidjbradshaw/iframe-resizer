import IframeResizer from './IframeResizer'

// `export { default }` is blocked by no-restricted-exports, so we import and re-export
// eslint-disable-next-line unicorn/prefer-export-from
export default IframeResizer
