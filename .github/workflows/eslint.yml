# This workflow uses actions that are not certified by GitHub.
# They are provided by a third-party and are governed by
# separate terms of service, privacy policy, and support
# documentation.
# ESLint is a tool for identifying and reporting on patterns
# found in ECMAScript/JavaScript code.
# More details at https://github.com/eslint/eslint
# and https://eslint.org

name: ESLint

on:
  push:
    branches: [ "master", "dev" ]
  pull_request:
    # The branches below must be a subset of the branches above
    branches: [ "master", "dev" ]
  schedule:
    - cron: '43 18 * * 4'

jobs:
  eslint:
    name: Scanning
    runs-on: ubuntu-latest
    permissions:
      contents: read
      security-events: write
    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: '26.x'
          cache: 'npm'

      - name: Use npm 12
        run: npm install -g npm@12

      - name: Install dependencies
        run: npm ci

      - name: Install ESLint
        run: npm install @microsoft/eslint-formatter-sarif@3

      - name: Run ESLint
        run: npm run eslint:sarif
        continue-on-error: true

      - name: Ensure SARIF file exists
        run: |
          if [ ! -f eslint-results.sarif ]; then
            echo '{"version":"2.1.0","runs":[]}' > eslint-results.sarif
          fi

      # The SARIF formatter includes messages silenced by eslint-disable
      # comments, and code scanning raises alerts for them anyway. Only
      # upload what ESLint actually reports.
      - name: Drop inline-suppressed results from SARIF
        run: |
          jq '.runs[].results |= map(select(((.suppressions // []) | length) == 0))' \
            eslint-results.sarif > eslint-results.filtered.sarif
          mv eslint-results.filtered.sarif eslint-results.sarif

      - name: Upload analysis results to GitHub
        uses: github/codeql-action/upload-sarif@v4
        with:
          sarif_file: eslint-results.sarif
          wait-for-processing: true
